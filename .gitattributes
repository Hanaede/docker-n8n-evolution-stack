# Force LF line endings for files that must run inside Linux containers
# or be parsed by Unix tooling. Prevents Git on Windows from converting
# them to CRLF on checkout (which breaks shell script shebangs).
*.sh        text eol=lf
*.sql       text eol=lf
*.env       text eol=lf
Dockerfile  text eol=lf
